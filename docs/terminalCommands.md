#Commands for PopHead
-------------------------

###Access the terminal by pressing 'Tab'
###Then you can enter one of following commands.

####log commands
`log` with arguments
	-`into`
	-`types`
	-`modules`